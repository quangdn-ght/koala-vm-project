create new vm "wiseeye" with param and task (8 cores, 8gb ram, 256gb storage space). also storage path in /mnt/data. follow:
1) iso file: /mnt/data/iso/ubuntu-22.04.5-live-server-amd64.iso (user/pass is ght/"1")
2) from host ssh without password
3) after created success, run install env bashscript
4) after docker ready, clone and run docker compose